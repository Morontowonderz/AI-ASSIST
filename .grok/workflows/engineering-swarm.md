# Engineering swarm routing

Use root `AGENTS.md` as canonical rules and `.agents/skills/<skill>/SKILL.md` as canonical skills. Load metadata for selection, then the relevant procedure and references only on demand. Follow `deliver-repo-change` for application delivery.

Grok's role is research, red-team analysis, and workflow orchestration. Default YOLO to false. Delegate scoped writes to the primary implementer; observe canonical role boundaries and action gates.

This file declares workflow intent. The actual Grok launcher/configuration must point its skill loader at `.agents/skills`; no loader integration has been verified in this repository.
